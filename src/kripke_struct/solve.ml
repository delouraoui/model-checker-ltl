open Schema
open Witness 
open Z3


       
let solve set loopLength beta gamma k exemple switch =
  assert false
  (**let lp = mk_varlist_pos [] ctx (beta+gamma) in
  let wl = mk_witness_args ctx varslist lp (beta+gamma) in
  witness ctx wl loopLength beta gamma**)
